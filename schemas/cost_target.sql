create table cost.accion_cost_target(
sku varchar,			
store_set_id varchar,
cost varchar,
future_cost varchar,
start_date varchar,
client_cost_id varchar,
project_id varchar,
ret_project_id varchar,		
upc varchar,		
last_seen varchar,		
end_date varchar
);
