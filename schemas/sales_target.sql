create table sales.accion_sales_target(
last_seen varchar,
sku varchar,			
store_set_id varchar,			
sales_type_id varchar,			
volume varchar,		
revenue	varchar,			
project_id varchar,			
ret_project_id varchar,			
upc varchar,		
margin	varchar,		
start_date varchar,		
end_date varchar
);
